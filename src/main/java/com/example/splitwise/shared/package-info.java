/**
 * Shared cross-cutting types.
 *
 * Only stable primitives such as money values, API errors, pagination contracts, and audit metadata
 * belong here; business rules remain owned by their feature module.
 */
package com.example.splitwise.shared;