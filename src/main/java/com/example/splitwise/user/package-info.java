/**
 * User profile and lookup module.
 *
 * User records are shared references for groups, expense payments, expense shares, and settlements;
 * this module owns profile data without exposing persistence entities through the API.
 */
package com.example.splitwise.user;